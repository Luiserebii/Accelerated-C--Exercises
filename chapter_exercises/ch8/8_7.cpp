/**
 *
 * Why doesn't the max function use two template parameters,
 * one for each argument type?
 *
 *
 * Likely to avoid entering the tricky solution where it must
 * compare two different values of different types.
 *
 **/
