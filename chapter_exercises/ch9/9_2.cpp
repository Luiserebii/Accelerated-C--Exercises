/**
 * 9-2. If we define the name function as a plain, nonconst member function,
 * what other functions in our system must change and why?
 *
 * const member functions have no restrictions in how they are used,
 * only in how they are implemented, so doing so is fine and will not
 * have an effect on other functions.
 */
